# Environment variables for embedding configuration

# Choose your embedding model: jina4, qwen3, openai
# Each model class defines its own MODEL_NAME and MAX_SEQ_LENGTH
EMBEDDING_MODEL=jina4

# MongoDB configuration
DATABASE_URI=mongodb://localhost:27017

# Optional: Remote MongoDB (comment out DATABASE_URI above to use)
# DATABASE_URI=mongodb+srv://username:password@cluster.mongodb.net
# QWEN3_MAX_SEQ_LENGTH=32768
# EMBEDDING_BATCH_SIZE=32
# EMBEDDING_DEVICE=auto  # auto, cpu, cuda, mps
